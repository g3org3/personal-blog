---
export interface Props {
	date: Date;
}
const { date } = Astro.props;
---

<time class="font-mono text-gray-500" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('fr-fr', {
			year: 'numeric',
		})
	}
</time>
